source ENV['GEM_SOURCE'] || 'https://rubygems.org/'

puppetversion = ENV['ENV_PUPPET_VERSION'].nil? ? '5.5.6' : ENV['ENV_PUPPET_VERSION'].to_s
gem 'puppet',  puppetversion, :require => false
gem 'puppet-lint',            :require => false
